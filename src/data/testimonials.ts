/**
 * DEVELOPMENT PLACEHOLDERS — NOT REAL CUSTOMER QUOTES.
 *
 * These strings exist so the paywall layout can be designed and tested.
 * They are rendered with an explicit "Example" tag in the UI and MUST be
 * replaced with verified, consented customer quotes (or removed) before any
 * production release. Fabricated testimonials in a shipping app violate
 * App Store / Play Store policy and consumer-protection law.
 */
export interface PlaceholderTestimonial {
  quote: string;
  attribution: string;
}

export const PLACEHOLDER_TESTIMONIALS: PlaceholderTestimonial[] = [
  {
    quote: 'The plan finally told me what to do instead of guessing every session.',
    attribution: 'Example quote — replace before release',
  },
  {
    quote: 'Logging is fast enough that I actually do it between sets.',
    attribution: 'Example quote — replace before release',
  },
  {
    quote: 'Seeing my lifts and weight trend in one place keeps me showing up.',
    attribution: 'Example quote — replace before release',
  },
];
